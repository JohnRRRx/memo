2/14
スキルチェック
Cランク　1問

-------------------------------------------

n, d = gets.split.map(&:to_i)
x, y = gets.split.map(&:to_i)

count = 0

n.times do
  xi, yi = gets.split.map(&:to_i)
  dis = (x - xi).abs + (y - yi).abs
  if dis <= d
     count += 1 
  end
end

puts count


問題をコードに落とし込むまでに、めっちゃ時間がかかった。
, を使えば複数の変数を指定できることをすっかり忘れて、別の方向で進んでしまった。
構造自体は変数の数が変わっただけなのに、理解するのに手間取った。
もっと練習が必要。
