2/7

スキルチェック
Cランク　1問

-------------------------------------------

平均値を出力するが、小数点以下切り上げ

a = gets.to_i
b = gets.split.map(&:to_i)
puts ((b.sum.to_f)/a).ceil

割る前の段階でfloatにしないと、整数のままになってしまう。
float型にしてから平均値を出したら、結果は小数点以下まで出る。
そこでceilメソッドで切り上げ。
